<nav class="nav">
  <div class="nav-logo">
    <h1>davru</h1>
  </div>
  <div class="nav-links">
    <a href="/" class="nav-link">
      <span class="nav-link-number">01</span>home
    </a>
    <a href="/blog" class="nav-link">
      <span class="nav-link-number">02</span>blog
    </a>
  </div>
</nav>

<style>
  .nav {
    width: 100%;
    padding: 1.75rem;
    position: absolute;
    color: var(--text-color);
    top: 0; left: 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .nav-logo h1 {
    color: var(--color-text-main);
    display: inline-block;
    font-size: 1.05em;
    font-weight: bold;
  }

  .nav-links {
    display: flex;
    align-items: center;
    font-weight: 500;
    font-size: .85em;
    gap: 1.5em;
  }

  .nav-link {
   	text-decoration: none;
    color: inherit;
    cursor: pointer;
    transition: all .2s ease;
  }

  .nav-link-number {
    font-size: 1em;
    letter-spacing: -1px;
    color: var(--color-text-main);
    margin-right: .5em;
  }

  .nav-link-number::after {
    content: '.';
    margin-left: -.2em;
  }

  @media (min-width: 600px) {
	  .nav {
	    padding: 1.8rem 2.2rem;
		}

  	.nav-links {
	    gap: 2.5em;
	    font-size: .95em;
    }

    .nav-link:hover {
      color: var(--color-text-main);
    }
  }
</style>
